# AegisRoute — IMPLEMENTATION_LOG

One line per change, appended at commit time. Format:

```
<commit subject or short hash> — <files/areas touched> — <why>
```

---

- `chore: initialize resumable project tracking` — PROJECT_STATE.md, TODO.md, IMPLEMENTATION_LOG.md, DECISIONS.md, README.md (minimal), .gitignore, .env.example, Makefile, go.mod, internal/*/doc.go, .gitkeep'd cmd//migrations//deploy//scripts//docs//observability//.github — establish the memory system, locked decisions, and repo skeleton before any code.
- `feat: foundation config, httperror, logging, metrics scaffold` — internal/config, internal/httperror, internal/observability, internal/metrics (+ tests), go.mod/go.sum deps — Stage 1 foundation packages; `make verify` green with no Docker.
- `heavier test on error boundaries within db migrations, empty json policy fallback issue` (be9c439) — migrations/*.sql (embedded goose, schema only), internal/models (+enums), internal/db (pool, repos, migrate, errors), internal/redisstore, gateway-api -migrate wiring, integration_test — Stage 2 data layer; unit tests Docker-free, integration behind //go:build integration.
- `feat: gateway server, middleware, auth, health, seed, models + admin control-plane` — internal/auth (HashAPIKey/BearerAuth/AdminAuth), internal/api (chi router, middleware chain, health/Pinger, /v1/models, admin CRUD), internal/seed (idempotent Run), cmd/gateway-api full serve/-seed + graceful shutdown, db repo additions (GetByHash→*APIKey, Backend.List/Upsert, Policy.GetByID/Upsert, IsUniqueViolation), Makefile seed-dev, chi/v5 dep — Stage 3 gateway core; all tests Docker-free with fakes.
- `feat: mock-llm, inference client, routing, circuit breaker, chat completions` (uncommitted; branch stage4_sync_inference_v2) — cmd/mock-llm (deterministic fake backend + httptest tests), internal/inference (Client.Do with timeout/retry/full-jitter backoff, typed transient-vs-permanent Error, per-attempt metrics), internal/routing (Breaker state machine + Selector with policy fallback, circuit skip, per-process max_in_flight semaphores, weighted priority tie-break), internal/db/inference_repo.go (+ integration subtest), internal/api chat.go (strict validation, canonical ChatRequest, X-AegisRoute-* headers, circuit reporting, best-effort ledger) + router/Deps wiring, cmd/gateway-api wiring, .env.example MOCK_* — Stage 4 synchronous inference path; all tests Docker-free (fake RoundTripper, fake selector/inference, httptest).
- `review hardening (uncommitted; fold into stage-4 commit)` — internal/inference/client.go (canceled outcome class, non-transient ctx-death, zero-base backoff), internal/routing/circuit.go (ReportCanceled probe return), internal/api/chat.go (case-sensitive raw-key strict validation top-level + per-message, cancel-aware circuit reporting, WithoutCancel+5s ledger ctx, forwardBody built before Select), internal/api/router.go (CircuitReporter.ReportCanceled), tests for all of the above, removed stray zz_scratch_review_test.go — fixes for five adversarial-review findings (circuit/metrics poisoning by client disconnects, lost audit rows on disconnect, case-insensitive strictness bypass, backoff doc mismatch).
- `Stage-4 polish (uncommitted; this session)` — internal/inference/client.go (MaxResponseBytes cap via io.LimitReader + ErrResponseTooLarge), internal/routing/selector.go (Select exclude ...uuid.UUID for failover), internal/config/config.go (ServerWriteTimeout, InferenceBudget, worst-case-fits-budget validator), internal/api/ledger.go (new AsyncLedger bounded async writer), internal/api/chat.go (failover loop, panic-safe callBackend, InferenceBudget ctx, Ledger.Record), internal/api/router.go (ChatSelector variadic, Deps.Ledger + InferenceBudget), cmd/gateway-api/main.go (wire AsyncLedger + budget + shared WriteTimeout), .env.example note, tests (chat failover/panic, e2e_test, ledger_test, selector exclusion, config validator, response cap) — six gateway-hardening items from the Stage-4 review; DoD green under -race, 254 test cases.
- `feat: response cache, idempotency, rate limiting on completion path` (uncommitted; branch stage4_sync_inference_v2) — internal/cache (Eligible/CanonicalBody/Key/Redis store + miniredis tests), internal/idempotency (Classify, IdempotencyStore, Coordinator, Scope + in-memory-fake tests), internal/db/idempotency_repo.go (atomic ON CONFLICT reclaim + integration subtest), internal/ratelimit (Lua fixed window + miniredis tests), internal/api (chat precedence rework: raw-body-once/raw hash/Check→limit→Begin→cache→infer→Complete-on-every-path, X-AegisRoute-Cache header, rateLimitMiddleware on /v1/models, ResponseCache/RateLimiter/IdempotencyGate Deps interfaces, 11 Stage-5 handler integration tests), cmd/gateway-api wiring, docs/design-decisions.md — Stage 5 request-path reliability controls; all Docker-free, green under -race.
- `Stage-5 verification fixes (uncommitted)` — internal/db/idempotency_repo.go (reclaim mints fresh id via `id = gen_random_uuid()` so a lapsed owner can't overwrite a reclaimed record; + Release), internal/idempotency (IdempotencyStore.Release + Coordinator.Release + tests), internal/api/chat.go (respondChat Completes <500 / Releases >=500 so transient 5xx aren't cached-and-replayed for the TTL; Idempotency-Key length cap 255), internal/api/router.go (IdempotencyGate.Release), test fakes + chat_stage5_test (retryable-error-releases, deterministic-4xx-replays, oversized-key-rejected) + db integration Release subtest, DECISIONS.md + docs/design-decisions.md — two end-to-end verification passes surfaced 2 critical idempotency correctness bugs + 1 hardening gap; all fixed, DoD green under -race.
- `Stage-6 verification fixes (uncommitted)` — internal/worker/worker.go (dropMissingJob: ack+DLQ a message whose job was deleted, instead of looping forever on jobs.ErrNotFound from MarkJobRunning/RecomputeAndUpdateJobStatus; markInFlight/isInFlight in-flight delivery tracking so reclaimOnce and handleMessage never double-process the same message in one process — prevents reclaim-induced premature item exhaustion for long jobs) + worker_test.go (missing-job-drop, in-flight guard, skip-in-flight, TestWorker_EndToEndOverStreamQueue running worker.Run over a real miniredis StreamQueue — closes the consume-loop coverage gap); internal/redisstore/queue.go (approximate MAXLEN ~100000 on the job and :dlq XADDs — XACK doesn't trim the stream, so it grew unbounded); internal/config/config.go (ValidateForWorker checks only stores + backend/retry/CB + stream + worker settings, not the gateway-only ADMIN_TOKEN/APP_KEY_HASH_SECRET/APP_PORT/RATE_LIMIT/CACHE/IDEMPOTENCY) + config_test.go; cmd/control-worker/main.go (use ValidateForWorker) — two end-to-end dataflow verification passes found 2 Major worker-durability bugs (poison-message infinite redelivery; reclaim self-race exhaustion) + 2 hardening gaps; all fixed, DoD green under -race.
- `feat: batch jobs, redis streams queue, control-worker with bounded pool and DLQ` (uncommitted; branch stage5_cache_iden_rate_limit) — internal/redisstore/queue.go (Queue interface + Message + Redis Streams StreamQueue: XADD, XGROUP MKSTREAM at offset 0, XREADGROUP no-auto-ack, XACK, XAUTOCLAIM, :dlq XADD) + memqueue.go (in-memory fake mirroring at-least-once, publish-error injection, DLQ/ack/inflight introspection) + queue_test/memqueue_test (miniredis publish→consume→ack, Claim recovers stranded via SetTime, DLQ, backlog-before-group); internal/jobs/status.go (pure ValidJobTransition/ValidItemTransition/AggregateJobStatus) + store.go (JobStore interface + ClaimResult/ClaimOutcome + jobs.ErrNotFound + ExhaustedError) + memstore.go (in-memory JobStore) + doc.go + status_test/memstore_test (transitions, aggregations, tenant scoping, concurrent-claim no-duplicate, exhaustion, terminal immutability, outbox lifecycle); internal/db/job_repo.go (JobRepo: transactional CreateWithItemsAndOutbox, FOR UPDATE SKIP LOCKED claim with claim-time exhaustion, WHERE status='running' terminal-immutable update, SQL status recompute mirroring AggregateJobStatus, RequeueRunningItems, outbox drain/marks; mapJobsNotFound→jobs.ErrNotFound; scanBatchJob/Item/Outbox) + integration_test.go (JobRepo + JobRepoClaimExhaustion subtests); internal/api/batch.go (POST create with 10 MiB cap, MVP schema validation reusing decodeChatRequest, same-model rule, 1..100/unique custom_id, Stage-5 idempotency precedence, one job-level publish, outbox-stays-pending on failure; tenant-scoped GET list/get/items) + router.go (BatchJobStore + JobPublisher Deps ifaces, batch routes in bearer group) + helpers_test.go (Jobs/JobQueue fakes) + batch_test.go (persist+one-publish, publish-failure keeps pending, GET/items/list, idempotency replay, cross-tenant 404, validation matrix); internal/worker/worker.go (bounded pool = WORKER_CONCURRENCY, per-item claim loop, shared routing+inference never-HTTP, durable write before Ack, Ack after RecomputeAndUpdateJobStatus, crash-recovery requeue, WORKER_MAX_ITEM_ATTEMPTS→one DLQ entry/item, XAUTOCLAIM reclaim ticker, outbox-drain ticker, panic-safe goroutines) + doc.go + worker_test.go (concurrency peak ≤ limit, redelivery skips terminal, Ack-after-durable via instrumented fakes, exhaustion→DLQ+job failed, partial-failure aggregation, outbox-drain retry-then-publish); cmd/control-worker/main.go (full wiring + /healthz + /metrics on WORKER_METRICS_PORT + graceful shutdown; removed .gitkeep) — Stage 6 asynchronous control-plane path; all Docker-free, DoD green (gofmt/vet/build/test), worker/redisstore/jobs/api also green under -race, `go build ./cmd/control-worker` builds.
- `feat: docker compose, prometheus, e2e, docs, CI — MVP complete` (uncommitted; branch stage6_batchjobs_redisstreams) — deploy/docker/{gateway-api,control-worker,mock-llm}.Dockerfile (multi-stage; pinned golang:1.25.11 build; static CGO_ENABLED=0 -trimpath -ldflags='-s -w'; gcr.io/distroless/static-debian12:nonroot final, non-root, no :latest) + .dockerignore; docker-compose.yml (postgres pg_isready+pgdata volume, redis redis-cli ping, gateway-api :8080 auto-migrate/seed + service-name DSNs, control-worker :9100, mock-llm-fast :8081 + mock-llm-cheap :8082 both llama-fast, prometheus :9090; no platform pins); observability/prometheus.yml (scrape gateway-api:8080 + control-worker:9100); scripts/e2e.sh (set -euo pipefail; preflight docker/curl/jq/go; cleanup trap down -v; clean-slate; gofmt/vet/test; up -d --build; bounded readiness waits; chat MISS→HIT; batch create→bounded terminal poll→items terminal; gateway+worker aegisroute_ metrics; live go test -tags=integration ./...); Makefile (real dev-up/dev-down/logs/verify-e2e); .github/workflows/ci.yml (unit gofmt/vet/test + integration PG/Redis services→migrate→integration tests); README.md (overview, not-a-chatbot, not-a-thin-proxy, mermaid, quickstart, LOCAL-ONLY creds, curl/cache-HIT/batch/metrics demos, Developer Operations, Assumptions & Tradeoffs, failure modes); docs/{architecture,api,failure-modes,resume-bullets,future-work}.md; removed obsolete .gitkeep placeholders — Stage 7 package/run/verify/document; MVP COMPLETE. Docker-free gate (gofmt/vet/test) green; make verify-e2e is the automated Docker gate.
- `Stage-7 verification fixes (uncommitted)` — scripts/e2e.sh (MAJOR: `docker compose stop control-worker` before the `go test -tags=integration` step so the live worker's outbox-drain/consume loops can't publish+claim the integration tests' jobs and race their PendingOutbox/ClaimNextQueuedItem assertions — PendingOutbox has no tenant filter, ~10% flaky window; OTHER: harden the bounded batch poll with `2>/dev/null … || true` so a transient curl/jq blip retries instead of aborting under `set -euo pipefail`); README.md (MINOR: go.mod declares `go 1.25.7` not `go 1.25`, plus a note that the pinned golang:1.25.11 build image is a patch ≥ the directive so no toolchain is downloaded) — two verification passes over the Stage-7 artifacts + packaged dataflow; verified all pinned images pull, compose config valid, promtool passes, CI YAML parses, and Prometheus scrapes both processes `up` and ingests the aegisroute_ series; `make verify-e2e` re-run PASSED after the fixes.
